"""
Evident Technologies — Single source-of-truth version.

All version references (package.json, app_config.py, docs) must
synchronize with this value.  Bump here first, then propagate.
"""

__version__ = "0.8.0"

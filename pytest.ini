[pytest]
testpaths = tests
pythonpath = .
norecursedirs = .git .venv .github site _site node_modules scripts quarantine
addopts = -q
markers =
    performance: Throughput and scalability benchmarks (may be slow)
